# Prompt feedback from the hand-run that produced this gold reference

Generated by the model that produced `context.json`, surfacing weaknesses in the prompts as it ran. Keep this file alongside the gold reference so future readers can see *what was wrong with the prompts at the time the gold reference was produced* — important context when evaluating diffs.

## Step 2 (Extractor)
Strong. The "empty is better than wrong" rule prevents hallucination; focus on source language is good. **No changes needed.**

## Step 3 (Vocabulary Normalizer)
**Contradiction:** "If the source uses a term but doesn't define it, either skip it or include it with an empty definition" offered two options without picking. **Patched 2026-05-03** — the prompt now requires inferred-marker inclusion via `[INFERRED — please confirm]` prefix, which mirrors the consulting move ("you mean Customer Success Manager, right?").

**Known issue (unpatched):** This gold run over-inferred — `CSA` was given a definition ("Likely abbreviation for Solutions Architect") and `CSM` was expanded to "Customer Success Manager" without source support. Future runs after the patch should include these with the `[INFERRED]` prefix instead of fabricating definitions inline.

## Step 4 (Unwritten Rules Sniffer)
**Conservatism gap:** The prompt's "be conservative" guidance was too soft — it produced 3 rules where the source genuinely supports maybe 2. **Patched 2026-05-03** — added "if you're hesitating about whether something qualifies, that hesitation is the answer: leave it out. Empty rules is a fine outcome."

**Known issue (logged for future):** Rule 2 of this gold output ("Enable customer self-sufficiency...") is a restatement of a formal Core Responsibility bullet, not a genuine unwritten rule. The "don't restate the formal stuff" guidance is too vague. Logged in `plans/04-future-considerations.md` for a stronger anti-restatement clause when we test against artifacts that surface this issue more often.

**Architectural note:** Job descriptions are *naturally* low on unwritten rules because they lack asides, examples, and dialogue. Empty `unwritten_rules` is correct behavior for a JD. To validate Step 4 actually fires, we need to test against a transcript or runbook eventually.

## Step 5 (Gap Reporter)
**Excellent.** The specificity requirement and probe_suggestion format produced 7 actionable, narrow probes. **No changes needed.** This is the strongest prompt in the pipeline.

## Step 6 (Confidence Scorer)
Clear and well-structured. The empty-fields-aren't-penalized guidance prevents punishing correct extraction. **No changes needed.**

Minor suggestion not applied: stating the empty-field rule first (before the score scale) might catch scorer bias earlier. Filed but not patched.

## Overall

Pipeline is well-designed. The main weakness is naturally artifact-dependent: formal documents produce thin `unwritten_rules` because the substrate doesn't exist. This is correct behavior, not a failure — and it's why `RoleContext` is *priors* for the discovery agent, not the final spec.
